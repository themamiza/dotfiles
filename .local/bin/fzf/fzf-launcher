#!/bin/sh

printf '%s\n' "$PATH" | tr ':' '\n' | while IFS= read -r dir; do [ -d "$dir" ] && find "$dir" -maxdepth 1 -mindepth 1 -type f -executable -printf '%f\n' 2>/dev/null; done | sort -u | fzf | xargs -r setsid -f
