hl.config({
    input = {
        kb_layout = "us, ir",
        kb_options = "grp:alt_shift_toggle",
        numlock_by_default = true,
        repeat_rate = 50,
        repeat_delay = 300,
        touchpad = {
            natural_scroll = true
        },
    },
    cursor = {
        inactive_timeout = 5,
        no_warps = true
    }
})
